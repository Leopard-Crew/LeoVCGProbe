# LVCP-FIND-0005: VCGlib PLY read probe on Leopard/PPC with GCC10

## Date

2026-05-16

## System

- Mac OS X Leopard 10.5.8
- PowerPC G5
- GCC 10.5.0 from quarantined helper toolchain:
  `/Users/admin/Desktop/Projekte/_helpers/toolchains/gcc10-bootstrap`

## Scope

This finding verifies whether VCGlib can import an ASCII PLY file on Leopard/PPC.

The input file is generated by the PLY write probe:

`build/vcg_triangle_write_probe.ply`

## Source layout

- Vendored source: `vendor/vcglib`
- Probe source: `src/vcg_ply_read_probe.cpp`
- Build harness: `build-gcc10.sh`
- Linked VCGlib helper source: `vendor/vcglib/wrap/ply/plylib.cpp`

## Build conditions

Eigen vectorization is disabled in the build harness:

```text
-DEIGEN_DONT_VECTORIZE
-DEIGEN_DONT_ALIGN_STATICALLY
-DEIGEN_MAX_ALIGN_BYTES=0
````

The GCC runtime is linked statically:

```text
-static-libstdc++ -static-libgcc
```

## Runtime result

```text
LeoVCGProbe PLY read probe ran.
Importer result: 0
Input path: build/vcg_triangle_write_probe.ply
Vertices: 3
Faces: 1
Bounding box min: 0, 0, 0
Bounding box max: 1, 1, 0
PLY read check: OK
```

## Runtime dependencies

```text
build/vcg_ply_read_probe:
    /usr/lib/libSystem.B.dylib
```

## Vendor policy

No files under `vendor/` were modified.

## Decision

VCGlib successfully imports an ASCII PLY triangle mesh on Leopard/PPC with the quarantined GCC10 scalar baseline.

The PLY path is now confirmed for:

- write
    
- read
    
- basic geometry validation after import
    

Next probes:

1. PLY write/read roundtrip in one dedicated probe
    
2. STL write/read
    
3. mesh cleaning functions
    
4. decimation  

